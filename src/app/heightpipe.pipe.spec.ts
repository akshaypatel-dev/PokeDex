import { HeightpipePipe } from './heightpipe.pipe';

describe('HeightpipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeightpipePipe();
    expect(pipe).toBeTruthy();
  });
});
